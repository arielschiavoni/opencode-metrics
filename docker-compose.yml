services:
  metrics-exporter:
    build: ./metrics
    volumes:
      # SQLite WAL mode uses three files: the main DB, a WAL (Write-Ahead Log) for new writes,
      # and a SHM (Shared Memory) index. All three must be mounted separately to prevent
      # virtiofs inode caching issues on macOS/Podman that can hide new data.
      - ${HOME}/.local/share/opencode/opencode.db:/data/opencode.db:ro
      - ${HOME}/.local/share/opencode/opencode.db-wal:/data/opencode.db-wal:ro
      - ${HOME}/.local/share/opencode/opencode.db-shm:/data/opencode.db-shm:ro
      - metrics_state:/state
    environment:
      - OPENCODE_DB=/data/opencode.db
      - STATE_DB=/state/metrics-state.db
      - PROMETHEUS_URL=http://prometheus:9090
      - POLL_INTERVAL_MS=60000
      - BATCH_SIZE=500
    depends_on:
      - prometheus
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v3.12.0
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=2y"
      - "--web.enable-remote-write-receiver"
      - "--web.enable-lifecycle"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:12.4.4
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/var/lib/grafana/dashboards/opencode.json
      - GF_DATE_FORMATS_INTERVAL_HOUR=DD/MM HH:mm
      - GF_DATE_FORMATS_INTERVAL_DAY=DD/MM
      - GF_DATE_FORMATS_INTERVAL_MONTH=MM/YYYY
      - GF_DATE_FORMATS_FULL_DATE=DD/MM/YYYY HH:mm:ss
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3100:3000"
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:
  metrics_state:
